# HuggingFace token (GigaChat3 is MIT — token optional, needed only for private datasets)
HF_TOKEN=

# Weights & Biases API key (optional, for experiment tracking)
WANDB_API_KEY=

# GPU selection (comma-separated for multi-GPU)
CUDA_VISIBLE_DEVICES=0
